# このファイルをコピーして .env を作成してください
# cp .env.example .env
# .env は .gitignore 対象のため、リポジトリにはコミットしないこと
#
# Vite の仕様上 VITE_ プレフィックスの変数はビルド後の JS に埋め込まれ、
# ブラウザから参照可能になる（Google OAuth のクライアントIDはそもそも公開情報のため問題ない）。

# ---- Google OAuth ----
# バックエンドの GOOGLE_OAUTH_CLIENT_ID と同じ値を設定する
VITE_GOOGLE_CLIENT_ID=
